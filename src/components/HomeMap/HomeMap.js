import React, { Component } from 'react'
import { connect } from 'react-redux'

class HomeMap extends Component {
    render() {
        return (
            <>
            <h1>Home Map Page</h1>
            </>
        )
    }
}

export default connect(HomeMap);